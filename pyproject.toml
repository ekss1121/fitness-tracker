[project]
name = "fitness-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.1",
    "click>=8.1.7",
]

[project.scripts]
fitness-tracker = "cli:main"

[tool.uv]
package = true
